{
  "name": "app-prototype-oidc",
  "version": "1.0.0",
  "description": "Aplicação que usa Open ID Connect para comunicar com o WSO2",
  "main": "index.js",
  "scripts": {
    "client": "cd client && npm start",
    "server": "node server.js",
    "dev": "concurrently --kill-others-on-fail \"npm run server\" \"npm run client\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "concurrently": "^3.5.0"
  },
  "author": "Vinicius Rodrigues",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.21.0",
    "body-parser": "^1.19.0",
    "express": "^4.17.1",
    "openid-client": "^4.2.1",
    "universal-cookie": "^4.0.3"
  }
}
